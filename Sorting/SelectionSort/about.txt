Selection Sort is a basic sorting algorithm.

It works by:

Finding the smallest element in the list

Placing it at the correct position

Repeating this for the remaining list

Each step selects the minimum value and fixes its position.